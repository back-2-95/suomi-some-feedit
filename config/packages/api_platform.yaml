api_platform:
    title: Suomi Some feedit
    version: 1.0.0
    show_webby: false
    enable_re_doc: false
    mapping:
        paths:
            - '%kernel.project_dir%/src/Entity'
    # Good defaults for REST APIs
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
